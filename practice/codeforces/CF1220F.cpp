#include<bits/extc++.h>
using namespace std;
const int maxn = 200000 + 5;
int n;
int a[maxn << 1];
signed main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    cin >> n;
    for (int i = 1; i <= n; i++)
    {
        
    }
    return 0;
}